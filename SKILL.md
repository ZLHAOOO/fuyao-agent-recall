---
name: fuyao-agent-recall
description: >
  自动联想引擎（Agent Recall Engine）。本地 BM25 匹配 + 自动 priming，解决 AI 助手"明明记过，却想不起来"的问题。
  MUST USE 除了以下情况，每轮对话都应执行此 Skill：
  (1) 简单打招呼（你好/hello/hi）、
  (2) 短回应（好/嗯/ok/收到/继续/谢谢）、
  (3) 纯闲聊/情绪表达（今天好累/随便聊聊）。
  其他所有对话——包括提问、讨论、创作、决策、调研、执行——都应在开始前运行 recall q "用户的问题"，
  让模型获得相关记忆的浅层提示。这不是用户发起的搜索，而是模型的自动唤起。
metadata:
  short-description: 自动联想引擎 - 除简单打招呼外，每轮对话自动唤起相关记忆
---

# Fuyao Agent Recall Engine

零成本本地联想引擎，让 AI 在对话中自动"灵光一现"记起相关记忆。

## 核心理念

**不是"用户让我搜"，而是"我自动知道"。**

这个工具的价值不在于精准搜索，而在于**在对的时刻给模型一个浅层提醒**："你的记忆库里可能有相关的信息"。就像人聊天时听到某个关键词后脑中自然浮现相关记忆——这个"浮现"是自动的，不需要主动发起。

## 安装

```bash
bash install.sh
```

- **pi 环境**：自动安装为 extension，重启后每轮无条件运行
- **OpenClaw**：检测 `~/.openclaw/workspace/` 目录结构
- **Hermes / Claude Code / Cursor**：检测当前工作目录的 `AGENTS.md` + `memory/`
- **其他 agent**：以 skill 模式运行，遵循下方触发规则

## 触发规则

### 每轮都运行（除了以下例外）

**例外情况（不需要运行）：**
- 简单打招呼：你好 / hello / hi / 嗨
- 短回应：好 / 嗯 / ok / okk / 收到 / 继续 / 谢谢 / 不客气
- 纯闲聊 / 情绪表达：今天好累 / 随便聊聊 / 无聊 / 好玩
- 少于 4 个字符的任何输入

**其他所有对话都需要运行：**
- 提问（"怎么写 X""为什么 X""X 是什么"）
- 讨论 / 分析（"帮我分析一下""对比一下"）
- 创作（"写一篇""帮我写"）
- 决策（"要不要""该不该""选哪个"）
- 调研 / 搜索（"查一下""搜一下"）
- 执行任务（"帮我做""执行""运行"）
- 技术问题（代码、配置、架构）
- 项目相关（进度、计划、问题）

### 运行方式

```bash
recall q "用户的完整问题"
```

如果命中，模型会看到"标题 + 一句话摘要 + 标签"。基于摘要判断：
- **摘要已足够** → 直接回答，不需要读文件
- **需要细节** → `grep -n "关键词" 文件路径` 定向搜索
- **不相关** → 忽略，正常回答

## 实体词典（名字 → 是谁）

对**人名 / agent 名 / 产品名 / 组织名**，BM25 的 n-gram 天生失灵：
中文两字名（阿橘、阿旺）抽不出 3-gram，混合名（Nova99）两头落空。
（v1.1 起 BM25 侧的 2 字词盲区已由 jieba 真词层补上；但实体卡的价值不止检索——
它是**身份层**：名字 → 是谁 → 详情在哪，这是 BM25 给不了的。）
所以实体识别**不走索引**，走一张独立词典：`memory/entities.md`（表格：名字 | 别名 | 类型 | 是谁 | 详情）。

**自动联动（零成本同步）**：词典每次查询直接读文件（~2ms），不进 BM25 索引——
**改 entities.md = 下一轮立刻生效**，不需要 `recall build`，索引坏了也不影响认名字。

命中时输出一张身份卡（独立于 BM25 结果，永远排在最前）：

```
👤 名字→是谁（只认身份，别展开）：
· 阿橘〔agent〕 老大构建的另一个数字伴侣，走八字/塔罗/大衍筮法…｜详情 relations.md
   ↳ memory/journal/2026-09.md:263 老大给了一份完整介绍（阿橘整理 · 2026-09-04）…
```

设计语义与人类一致：**听到名字 → 知道是谁 → 需要时才展开**。卡片只给身份和原文指针，不给结论。

**三条纪律**（注入提示词只带压缩版，详细语义以本节为准）：
1. 👤 命中 → 只取身份，不展开详情（除非本轮任务真的需要）
2. 词典未命中 → 照常走 BM25；查到新实体就**当场补进 entities.md**（学习动作）
3. 全库都没有 → 明确说"不认识"，**不猜不编**

日常用语（老大/自己/我们/客户）登记了也不出卡（GENERIC 表），避免每轮刷屏。

## 实体审计（反向联动）

词典最大的风险是**不知道自己漏了什么**。`audit` 扫全库，找「反复出现但不在词典」的候选：

```bash
recall audit --min 3 --top 25
```

候选来源：`**加粗**`（排除 `**字段名**：` 这类模板标签）、「引号」、@handle。
**只建议，绝不自动写入**——收录判断是 Agent 自己的活（一次性人名、概念词不收）。

## 约束

注入提示词为一句英文（对任意 harness/模型可移植），语义：

1. 先用 `grep -n` 关键词定位，只读命中行附近 ±5 行，禁止读整篇文件
2. 👤 身份卡只取身份，任务真需要细节才展开
3. 不相关直接忽略，不要强行关联；命中 0 条时完全沉默

## 数据位置

- 索引文件：`memory/.recall-index.json`（在检测到的 Agent 目录下）
- 自动检测：pi / OpenClaw / Hermes / Claude Code / Cursor 的目录结构
- 索引自愈：检测到新文件自动重建
- 手动重建：`recall build`
- 实体审计：`recall audit --min 3`（候选清单，人工收录）

## 技术

- **算法**：BM25（k1=1.5, b=0.75）
- **延迟**：90-130ms（含实体词典层，SQLite 点查）
- **中文真词层（v1.1）**：build 时 jieba 分词，词性过滤（名词系）+ entities.md 自动注入自定义词典，**只收 2 字真词**（3+ 字与 n-gram 天然重合，2 字才是盲区）；查询侧用 2-gram 补丁命中，**查询路径零 jieba 依赖**（运行环境不装 jieba 也能查询）
- **索引存储**：SQLite 点查（`memory/.recall-index.db`，倒排按词 IN 批量查）；文档元数据 pickle 存 meta 表；不再整体加载索引（旧 15.8MB JSON 全量加载曾占 466ms）
- **build 耗时**：jieba 分词后约 15-20s（离线任务，手动/自动触发均可）；未装 jieba 自动降级纯 n-gram
- **长文件切块**：索引时按 `##` 标题把长文件切成块级文档（title = 文件标题 › 小节标题），消除正文 8000 字符截断造成的静默失忆；命中粒度=条目级；同一文件的块一次最多露 2 条
- **成本**：零 API 调用
- **字段权重**：title×3 / tag×2 / body×1
- **注入上限**：5 条 / 700 字符（含实体身份卡，卡永远排最前）
- **实体词典**：`memory/entities.md`，查询时直读（~2ms），与索引解耦
- **通用 3-gram 过滤**：BIG_STOP 表过滤"的时候""的问题""的方式"等 30+ 个无信息量组合
- **强命中过滤**：查询含强词（英文标识符或 4-gram 中文）时，纯弱命中文档需 `score ≥ max(1.2, best×0.65)` 才入选，减少误报；强词命中**无论落在 title/tag 还是正文**都算强（正文精确短语匹配是高精度信号）

## 文件结构

```
fuyao-agent-recall/
├── SKILL.md              # 本文件
├── bin/recall            # BM25 匹配引擎
├── extensions/
│   └── recall-primer.ts  # pi 专用扩展
├── install.sh            # 环境检测 + 安装
└── README.md             # 文档
```

## License

MIT
